{
    "$schema": "https://vercel.com/schema.json",
    "installCommand": "corepack enable && pnpm install --frozen-lockfile --prefer-offline --shamefully-hoist --filter=./PACKAGES/** --filter=./APPS/WEB_APPS/amebogist...",
    "buildCommand": "pnpm -w run vercel:amebogist",
    "outputDirectory": "APPS/WEB_APPS/amebogist/.next",
    "framework": "nextjs",
    "env": {
        "NODE_ENV": "production"
    }
}