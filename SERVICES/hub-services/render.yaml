name: boldmind-hub-services
type: web
runtime: node
plan: free
autoDeploy: true
buildCommand: pnpm install && pnpm build
startCommand: pnpm start
envVars:
  - key: PORT
    value: 4001
  - key: SERVICE_NAME
    value: hub-service
domains:
  - hub.boldmind.ng